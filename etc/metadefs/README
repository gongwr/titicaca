This directory contains predefined namespaces for Everest Metadata Definitions
Catalog. Files from this directory can be loaded into the database using
db_load_metadefs command for everest-manage. Similarly you can unload the
definitions using db_unload_metadefs command.
